<?xml version="1.0" encoding="utf-8"?>
<!-- Inflated (merge) into com.ttcoachai.views.RepCarouselView, a vertical LinearLayout:
     prev/next arrows around a snap-paged RecyclerView, a "Rep N · flagged/clean" caption,
     and a dot page indicator. -->
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageButton
            android:id="@+id/btn_rep_carousel_prev"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="@drawable/bg_link_icon_tile"
            android:src="@drawable/ic_chevron_left"
            app:tint="@color/ttc_text_1"
            android:padding="7dp"
            android:contentDescription="@string/cd_rep_carousel_prev" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_rep_carousel_pages"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:overScrollMode="never"
            android:clipToPadding="false"
            tools:listitem="@layout/item_rep_carousel_page" />

        <ImageButton
            android:id="@+id/btn_rep_carousel_next"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:background="@drawable/bg_link_icon_tile"
            android:src="@drawable/ic_chevron_right"
            app:tint="@color/ttc_text_1"
            android:padding="7dp"
            android:contentDescription="@string/cd_rep_carousel_next" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_rep_carousel_caption"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.TTC.Body.Secondary"
        tools:text="Rep 7 · flagged" />

    <LinearLayout
        android:id="@+id/rep_carousel_indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:orientation="horizontal" />

</merge>
